summary using iptables:

# ./bip324-proxy.py
# groupadd --system redirect_to_bip324-proxy
# iptables -t nat -A OUTPUT -p tcp --dport 8333 -m owner --gid-owner redirect_to_bip324-proxy -j DNAT --to-destination 127.0.0.1:1324

start the light client software with

# sudo -g redirect_to_bip324-proxy <command ....>

delete rule again:
# iptables -t nat -D OUTPUT -p tcp --dport 8333 -m owner --gid-owner redirect_to_bip324-proxy -j DNAT --to-destination 127.0.0.1:1324


nftables (TODO, untested; does it have a 'owner' module as well???):

# nft add rule ip nat OUTPUT 'dnat to tcp dport map { 8333 : 127.0.0.1 . 1324 }'
